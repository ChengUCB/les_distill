# Water MLIPs — RPBE-D3 sampled configs

Trained MACE models from water configurations sampled via RPBE-D3 DFT.

## Layout

- `models/` — final `*_stagetwo.model` files.

## Notes

- Total final models: 6.
- Checkpoint models were intentionally excluded.
